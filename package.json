{
  "name": "netraluiswhisper",
  "version": "0.0.1",
  "description": "Local push-to-talk voice dictation for macOS (Wispr Flow style). Spike.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "keys": "DEBUG_KEYS=1 npm run start"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.10.0",
    "electron": "^33.2.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.22.2",
    "uiohook-napi": "^1.5.4"
  }
}
